# list of domains to support forcing redirect to www.
# If left blank, all requests will have a 'www.' prefix added to the request.
DOMAINS = [
]

# whether to redirect to https. If False, the redirect will take into account
# the scheme of the request.
FORCE_HTTPS = True
